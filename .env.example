# This file is an example and should be copied to .env.local for development.
# IMPORTANT: Do not commit .env or .env.local files to your repository.

# Firebase Client SDK Configuration
# These keys are public and are used by the client-side Firebase SDK.
# Find these in your Firebase Project Settings > General > Your Apps > Web App.
NEXT_PUBLIC_FIREBASE_API_KEY="AIza..."
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-project-id.firebaseapp.com"
NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id"
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-project-id.appspot.com"
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="..."
NEXT_PUBLIC_FIREBASE_APP_ID="1:..."
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID="G-..."

# Firebase Admin SDK Configuration (Service Account)
# These keys are private and MUST be kept secret. They grant admin access to your Firebase project.
# Find these by generating a new private key in Google Cloud IAM > Service Accounts.
# The FIREBASE_PRIVATE_KEY must be enclosed in quotes.
FIREBASE_PROJECT_ID="your-project-id"
FIREBASE_CLIENT_EMAIL="firebase-adminsdk-...@your-project-id.iam.gserviceaccount.com"
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Genkit / Gemini API Key
# This key is for using Google's Generative AI models.
# Obtain this from Google AI Studio.
GEMINI_API_KEY="AIza..."
